'use client';

import { useState, useTransition } from 'react';
import Link from 'next/link';
import { ArrowRight, HandCoins, Loader2, Store } from 'lucide-react';
import { registerMerchantFromReferral } from '@/app/actions/merchant-registration';

export default function ReferralRegistration({ referralCode }: { referralCode: string }) {
  const [isPending, startTransition] = useTransition();
  const [form, setForm] = useState({ storeName: '', username: '', password: '', email: '', referralCode });
  const [notice, setNotice] = useState('');
  const submit = (event: React.FormEvent) => { event.preventDefault(); startTransition(async () => { const result = await registerMerchantFromReferral(form); if (result.success) setNotice('تم إنشاء متجرك التجريبي بنجاح. يمكنك تسجيل الدخول الآن.'); else setNotice(result.error || 'تعذر إنشاء المتجر.'); }); };
  return <main dir="rtl" className="grid min-h-dvh place-items-center bg-zinc-950 p-4 text-zinc-100"><section className="w-full max-w-lg rounded-2xl border border-zinc-800 bg-zinc-900/45 p-5 shadow-2xl sm:p-7"><Link href="/login" className="inline-flex items-center gap-2 text-xs font-bold text-zinc-300 hover:text-white"><ArrowRight className="h-4 w-4" />لدي حساب بالفعل</Link><div className="mt-6 flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-500 text-zinc-950"><HandCoins className="h-5 w-5" /></span><div><h1 className="text-xl font-black">أنشئ متجرك من دعوة صديق</h1><p className="mt-1 text-xs leading-6 text-zinc-300">تبدأ بفترة تجريبية، ويحصل صديقك على عمولة عندما تجدّد اشتراكك.</p></div></div>{notice ? <p className={`mt-5 rounded-xl border px-3 py-3 text-sm font-bold ${notice.startsWith('تم ') ? 'border-emerald-500/30 bg-emerald-500/10 text-emerald-100' : 'border-rose-500/30 bg-rose-500/10 text-rose-100'}`}>{notice}{notice.startsWith('تم ') ? <Link href="/login" className="mr-2 underline">تسجيل الدخول</Link> : null}</p> : null}<form onSubmit={submit} className="mt-6 grid gap-4"><label className="text-xs font-bold text-zinc-200">اسم النشاط<input required value={form.storeName} onChange={(event) => setForm({ ...form, storeName: event.target.value })} placeholder="مثال: متجر خالد الرقمي" className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-3 py-3 text-sm text-white outline-none focus:border-emerald-400" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-bold text-zinc-200">اسم المستخدم<input required dir="ltr" value={form.username} onChange={(event) => setForm({ ...form, username: event.target.value })} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-3 py-3 text-sm text-white outline-none focus:border-emerald-400" /></label><label className="text-xs font-bold text-zinc-200">كلمة المرور<input required type="password" dir="ltr" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-3 py-3 text-sm text-white outline-none focus:border-emerald-400" /></label></div><label className="text-xs font-bold text-zinc-200">البريد الإلكتروني (اختياري)<input type="email" dir="ltr" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-3 py-3 text-sm text-white outline-none focus:border-emerald-400" /></label><label className="text-xs font-bold text-zinc-200">رمز الإحالة<input required dir="ltr" value={form.referralCode} onChange={(event) => setForm({ ...form, referralCode: event.target.value.toUpperCase() })} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-950 px-3 py-3 font-mono text-sm text-white outline-none focus:border-emerald-400" /></label><button disabled={isPending} className="mt-2 inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-emerald-500 px-4 text-sm font-black text-zinc-950 transition-colors hover:bg-emerald-400 disabled:opacity-60">{isPending ? <Loader2 className="h-4 w-4 animate-spin" /> : <Store className="h-4 w-4" />}إنشاء المتجر التجريبي</button></form></section></main>;
}
